import { styled } from "@mui/material/styles";
import TextValidator from "react-material-ui-form-validator";

const TextValidatorField = styled(TextValidator)(() => ({
  width: "100%",
  marginBottom: "16px",
}));

export { TextValidatorField };
